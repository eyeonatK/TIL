Python 변수 및 자료형 (Variables & Data Types)1. 개요 및 기본 문법주석 (Comments)단일 라인 주석: #여러 줄 주석: 따옴표 3개 (""" 또는 ''')출력문 (print)콘솔 출력 함수: print("출력 내용")변수 (Variables)값을 저장하는 메모리 공간의 식별자파이썬은 동적 타입 언어로, 실행 시점에 값에 따라 타입이 자동 추론됩니다.구분정적 타입 언어 (Static Type)동적 타입 언어 (Dynamic Type)타입 결정 시점컴파일 타임 (타입 선언 필수)런타임 (값 할당 시 자동 추론)대표 언어Java, C, C++, RustPython, JavaScript2. 기본 자료형 (Scalar Types)자료형식별자예시특징정수형int10, -5, 08진수(0o34), 16진수(0x2A) 지원실수형float3.14, -1234.5부동소수점 표현문자열str"hello", 'python'인덱싱 지원, 불변 객체(Immutable)불리언boolTrue, False논리 참/거짓널 타입NoneTypeNone값이 없음을 명시문자열 인덱싱0번부터 시작하는 인덱스를 가집니다.내부 요소를 직접 수정할 수 없는 불변(Immutable) 객체입니다.3. 복합 자료형 (Collections)자료형생성 기호인덱스(순서)수정 가능 여부중복 허용List[ ]지원수정 가능 (Mutable)허용Tuple( )지원수정 불가 (Immutable)허용Dict{ }키 매핑수정 가능Key 중복 불가Set{ }미지원수정 가능중복 자동 제거List 주요 메서드.append(x): 맨 뒤에 값 추가.insert(i, x): 인덱스 i 위치에 값 추가.remove(x): 지정 값 삭제.pop(): 마지막 값 추출 및 삭제.sort(): 정렬Tuple 특징단일 요소 정의 시 콤마 표기 필수: (1,)List보다 메모리 소모가 적고 속도가 빠름Dictionary 주요 메서드.get(key): 값 조회 (키 부재 시 None 반환).keys(): Key 목록 조회.values(): Value 목록 조회.items(): (Key, Value) 쌍 조회.clear(): 전체 데이터 삭제Set 주요 메서드.add(x): 값 추가.discard(x): 값 삭제 (키 없어도 에러 없음).remove(x): 값 삭제 (키 없으면 에러 발생).clear(): 전체 삭제4. 참고 레퍼런스점프 투 파이썬: https://wikidocs.net/book/1프로그래머스 코딩테스트: https://school.programmers.co.kr/learn/challenges?order=recent